name: baf-bot
services:
  - name: whatsapp
    dockerfile_path: ./Dockerfile
    ports:
      - internal_port: 3000
        protocol: HTTP
      - internal_port: 5678
        protocol: HTTP
    env:
      WAHA_API_KEY: "123456"
      N8N_PORT: "5678"
