name: baf-whatsapp-bot

services:
  - name: baf-service
    dockerfile_path: Dockerfile
    ports:
      - internal_port: 3000
        protocol: HTTP
      - internal_port: 5678
        protocol: HTTP
    envs:
      - key: WAHA_API_KEY
        value: 123456
      - key: N8N_PORT
        value: 5678